name: Spellcheck
on:
  push:
    paths:
      - "thesis/*"

jobs:
  build:
    name: Spellcheck
    runs-on: ubuntu-latest
    steps:
      # The checkout step
      - uses: actions/checkout@v2
      - uses: rojopolis/spellcheck-github-actions@0.11.0
        name: Spellcheck
